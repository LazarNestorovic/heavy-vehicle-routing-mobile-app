name: hvr_mobile
description: Heavy Vehicle Routing - vozacka mobilna aplikacija (diplomski rad)
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  web_socket_channel: ^3.0.3
  shared_preferences: ^2.5.5
  google_fonts: ^8.2.0
  google_maps_flutter: ^2.12.3
  google_sign_in: ^7.2.0
  geolocator: ^14.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
