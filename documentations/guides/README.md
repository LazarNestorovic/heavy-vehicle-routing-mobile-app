# Guides

Operativna uputstva "kako uraditi X" — pokretanje stack-a, rebuild grafa, dodavanje novog regiona, itd. Za razliku od `fixes/` i `decisions/`, ovi fajlovi **nemaju datum u imenu** i ažuriraju se in-place kad se procedura promeni (jer opisuju trenutno stanje procesa, ne istoriju).
